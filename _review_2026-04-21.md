# juhuo v2.0 Review（顾庸x 2026-04-21）

## 🔴 P0 必须修

### P0-1: 三个SQLite散落，互不关联
| 文件 | DB路径 | 用途 |
|------|--------|------|
| closed_loop.py | data/evolutions/juhuo.db | causal_chain + dimension_beliefs + judgment_snapshots + outcome_predictions + evolution_validation |
| self_evolover.py | data/judgment_data/jujuo_judgment.db | self_model + evolution records |
| experiences.py | data/judgment_data/jujuo_judgment.db | experiences表 |

跨DB事务不可能，数据不一致是必然的。
→ **一个项目一个DB：data/juhuo.db，所有表放一起。_get_db_conn()只定义一次。**

### P0-2: 文件名拼写错误
`self_evolover.py` 少了个v。两处（judgment/ 和 subsystems/judgment/）都拼错。
→ **全局改名 self_evolver.py，一次性搞定。**

### P0-3: router.py 仍是 30KB 单体
v2.0 拆了 llm_calls.py（307行），但 router.py 还是 877行/30KB。
→ **router.py 只做编排，每个注入源独立：**
```python
def check10d(task, profile=None):
    ctx = JudgmentContext(task, profile)
    ctx = inject_biography(ctx)
    ctx = inject_experiences(ctx)
    ctx = inject_correlation_memory(ctx)
    ctx = inject_emotion(ctx)
    return llm_judge(ctx)
```

## 🟡 P1 架构改进

### P1-4: 表定义分散在业务代码
behavior_logger.py 在 experiences 表上加列，但 experiences.py 定义的表结构里没有这些列。
→ **表定义统一在一个地方（schema.py 或 migration 脚本）**

## 🟡 P2 功能改进

### P2-5: biography.py 正则脆弱
26个正则只能匹配"32岁"，不能匹配"三十多岁"、"快30了"、"90后"。
一次正则扫描就写入DB，没有置信度。
→ **正则提取 → 候选事实列表 → LLM验证 → 确认后写入**

### P2-6: experiences find_similar 是 bigram 匹配
"要不要辞职"和"该不该离职"的 bigram 不同但语义相同。
→ **短期：关键词+situation_type；长期：embedding 向量检索**

### P2-7: action_executor 三通道无超时保护
Hermes/Claude Code CLI 没有 timeout、死循环检查、失败重试。
→ **加 timeout + 返回码检查 + 失败重试一次**

### P2-8: behavior 数据流断裂
behavior_logger 记录了工具调用，但下游没有消费。
→ **behavior 数据回流 experiences，工具调用成功/失败 → 自动生成 experience 条目**

### P2-9: MCP 服务器手工 JSON-RPC
手动拼 JSON-RPC 代码冗余。
→ **pip install mcp，用 FastMCP 重写（参考 blender-mcp）**

## 🟢 P3 可改进

### P3-10: life_os 可读性差
变量名全是缩写（EH/EM/EB/LS/ST/_dp/_ce/_rs/_ts）。
PAD 情绪只有6个词（开心/焦虑/平静/低落/兴奋/疲惫），无愤怒/厌恶/惊喜/无聊。
→ **展开变量名；PAD 检测词扩展到 30+**

### P3-11: experiences 加 embedding（长期）
→ **语义向量检索替代 bigram 匹配**

---

## 优先级总结

| 优先级 | 改动 | 工作量 |
|--------|------|--------|
| P0 | 合并三个 SQLite 为一个 | 半天 |
| P0 | 改名 self_evolver（拼写） | 10分钟 |
| P0 | router.py 编排模式重构 | 1天 |
| P1 | 表定义统一到 schema | 半天 |
| P2 | biography 加 LLM 验证 | 半天 |
| P2 | behavior 数据回流 experiences | 半天 |
| P2 | FastMCP 重写 mcp_server | 1小时 |
| P3 | life_os 展开 + 扩展 PAD 词 | 2小时 |
| P3 | experiences 加 embedding | 1天 |

---

_记录时间：2026-04-21 顾庸x review_
